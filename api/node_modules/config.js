let secrets = require('secrets')

let config = {
	imageDBpath: '/imageDB/',
	port: 9898,
	...secrets
}

config.uri = `mongodb+srv://${config.mongodb.user}:${config.mongodb.pwd}@cluster0.pjx4h.mongodb.net/${config.mongodb.dbName}?retryWrites=true&w=majority`

let GOOGLE_APPLICATION_CREDENTIALS = "./googleCredentials.json"

if(process.cwd().indexOf("wasteManagers") == process.cwd().length - "wasteManagers".length) GOOGLE_APPLICATION_CREDENTIALS = "wasteAPI/" + GOOGLE_APPLICATION_CREDENTIALS

process.env.GOOGLE_APPLICATION_CREDENTIALS=GOOGLE_APPLICATION_CREDENTIALS

if(process.cwd().indexOf("wasteManagers") == process.cwd().length - "wasteManagers".length) config.imageDBpath = "/wasteAPI"+config.imageDBpath


module.exports = config


